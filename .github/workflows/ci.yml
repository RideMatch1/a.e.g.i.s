name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2 — SHA-pinned per v0.16.3 D-CA-003; Node-24 runner-support per v0.17.3 SC-2 (Node-20 deprecation 2026-06-02)
      - name: Supply-chain — enforce GHA action SHA-pin policy (closes tj-actions-class)
        run: bash scripts/check-gha-sha-pin.sh
      - uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320  # v5.0.0 (runs.using=node24) — SHA-pinned per v0.16.3 D-CA-003; plan-doc §5.3 confirms the SHA has been on v5 since initial pinning (this commit-fix corrects the stale v4.3.0 comment without touching the pin)
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0 — SHA-pinned per v0.16.3 D-CA-003; Node-24 per v0.17.3 SC-2
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Supply-chain — dep cooldown lint (closes rushed-update window)
        run: bash scripts/check-dep-cooldown.sh
      - run: pnpm build
      - name: Supply-chain — dist/ planning-artifact leak gate (audit L1)
        run: bash scripts/check-dist-codename-leak.sh
      - name: Lint — real type-drift gate across 5 shipped packages (v0.16.3 D-CA-002)
        run: pnpm lint
      - run: pnpm test
      - name: Benchmark (21 planted vulnerabilities + 9 clean-file FP checks)
        run: pnpm benchmark
