// Sanitized from real 2026-04-29 dogfood-scan FP (NewTurn2017/nextjs-saas-kit
// app/api/auth/[...nextauth]/route.ts). NextAuth v5+ canonical handler shape.
// The route IS the auth machinery — there's nothing to "guard".

import { GET, POST } from "@/lib/auth"

export { GET, POST }
