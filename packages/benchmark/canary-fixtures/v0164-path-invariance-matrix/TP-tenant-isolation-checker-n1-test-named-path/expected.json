{
  "id": "v0164-TP-tenant-isolation-checker-n1-test-named-path",
  "cwe": 639,
  "type": "TP",
  "description": "Per-scanner path-invariance contract for tenant-isolation-checker — N1-class path. Route file at src/app/api/test/route.ts calls .from('users').select('*') without a tenant-boundary filter in the query chain. Scanner must flag CWE-639.",
  "expected": [
    { "scanner": "tenant-isolation-checker", "cwe": 639 }
  ]
}
